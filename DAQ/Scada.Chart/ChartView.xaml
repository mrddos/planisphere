<UserControl x:Class="Scada.Chart.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Scada.Chart"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"  MouseMove="MainViewMouseMove">
    <Grid x:Name="MainView">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel x:Name="ChartContainer" Grid.Column="0" Grid.Row="0" 
                    Orientation="Vertical"
                    MouseMove="MainViewMouseMove">
            
            <local:CurveView Background="GreenYellow" Height="210"></local:CurveView>

            <local:CurveView Background="AliceBlue" Height="200"></local:CurveView>
            
        </StackPanel>
        
    </Grid>
</UserControl>
