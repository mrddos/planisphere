<UserControl x:Class="Scada.Chart.CurveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200" Background="White"
             Loaded="CurveViewLoaded" MouseMove="CanvasViewMouseMove">
    <Border BorderBrush="Gray" BorderThickness="0.5">
        <Grid x:Name="MainView" >
            <Grid.RowDefinitions>
                <RowDefinition Height="10"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="10"></RowDefinition>
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <!-- Canvas -->
            <Canvas x:Name="Graduation" Grid.Row="1" Grid.Column="0" Background="White">
            </Canvas>

            <Canvas x:Name="CanvasView" Background="AliceBlue" Grid.Row="1" Grid.Column="1" MouseMove="CanvasViewMouseMove">
            </Canvas>
        </Grid>
    </Border>
</UserControl>
